<Window x:Class="LocalNEXUS.App.Views.AddExtensionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:LocalNEXUS.App.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:AddExtensionViewModel}"
        Title="{Binding Title}"
        SizeToContent="Height"
        Width="520"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Surface.Window.Brush}"
        Foreground="{DynamicResource Text.Primary.Brush}"
        FontFamily="{StaticResource Font.Sans}"
        TextOptions.TextFormattingMode="Ideal">

    <StackPanel Margin="22,18,22,18">
        <TextBlock Style="{StaticResource SectionHeader}" FontSize="{StaticResource Font.Size.Title}" Text="{Binding Title}" />
        <TextBlock Style="{StaticResource HintText}" Text="{Binding Explanation}" TextWrapping="Wrap" Margin="0,4,0,0" />

        <TextBlock Style="{StaticResource SectionHeader}" Text="{Binding ValueLabel}" Margin="0,16,0,0" />
        <TextBox x:Name="ValueBox" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Style="{StaticResource HintText}" Text="{Binding ValueHint}" Margin="0,4,0,0" />

        <StackPanel Visibility="{Binding IsCommand, Converter={StaticResource BooleanToVisibility}}">
            <TextBlock Style="{StaticResource SectionHeader}" Text="Name" Margin="0,14,0,0" />
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Style="{StaticResource SectionHeader}" Text="Arguments" Margin="0,14,0,0" />
            <TextBox Text="{Binding Arguments, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Style="{StaticResource HintText}" Text="Separated by spaces." Margin="0,4,0,0" />

            <TextBlock Style="{StaticResource SectionHeader}" Text="Working directory" Margin="0,14,0,0" />
            <TextBox Text="{Binding WorkingDirectory, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Style="{StaticResource HintText}" Text="Leave blank to use the extension folder." Margin="0,4,0,0" />

            <TextBlock Style="{StaticResource SectionHeader}" Text="Environment" Margin="0,14,0,0" />
            <TextBox Text="{Binding Environment, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     Height="56"
                     VerticalScrollBarVisibility="Auto" />
            <TextBlock Style="{StaticResource HintText}" Text="One per line, as NAME=value." Margin="0,4,0,0" />

            <TextBlock Style="{StaticResource SectionHeader}" Text="What it speaks" Margin="0,14,0,0" />
            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                <CheckBox Content="Tools a model can call" IsChecked="{Binding SpeaksMcp}" />
                <CheckBox Content="Node types for the graph" Margin="16,0,0,0" IsChecked="{Binding SpeaksNode}" />
            </StackPanel>
            <TextBlock Style="{StaticResource HintText}"
                       Margin="0,4,0,0"
                       TextWrapping="Wrap"
                       Text="Pick at least one, or there is no way to talk to it." />
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="CancelButton" Content="Cancel" Margin="0,0,8,0" MinWidth="88" />
            <Button x:Name="AddButton"
                    Content="Add"
                    MinWidth="88"
                    IsDefault="True"
                    IsEnabled="{Binding CanAccept}" />
        </StackPanel>
    </StackPanel>
</Window>
